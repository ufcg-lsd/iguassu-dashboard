const USER_ID = "user_id";
const USER_ALIAS = "user_alias";
const AUTH_STATE = "auth_state";
const CREDENTIALS = "credentials";

export default {
  USER_ID,
  AUTH_STATE,
  USER_ALIAS,
  CREDENTIALS
};
